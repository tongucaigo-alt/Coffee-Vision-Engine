name: coffee_source_dataset
description: Strict Source Catalog release JSON adapter for Atlas.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  atlas_canonical_json:
    path: ../atlas_canonical_json
  coffee_source:
    path: ../coffee_source

dev_dependencies:
  json_schema: ^5.2.2
  test: ^1.31.0
