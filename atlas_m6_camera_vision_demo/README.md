# Atlas M6 Camera Vision Demo

Milestone-specific Flutter validation app for the existing `coffee_camera` and
`coffee_vision` packages. It captures a mandatory cup and saucer pair, then
runs the frozen Coffee Vision detailed pipeline sequentially for both images.

This app is not a production application and does not add analysis algorithms,
thresholds, AI, symbol interpretation, or fortune behavior.
