name: atlas_canonical_json
description: Restricted RFC 8785 canonical JSON and SHA-256 profile for Atlas.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.31.0
